// SciText-ES frontend configuration.
// If you serve the frontend separately from the backend, set the API base URL here.
// Example:
// window.SCITEXT_API_BASE = "http://localhost:5000";
// window.SCITEXT_API_BASE = "https://tu-backend.example.com";

// Leave empty to let api.js use window.location.origin (same-origin backend) or fallback to localhost.
window.SCITEXT_API_BASE = window.SCITEXT_API_BASE || "";
